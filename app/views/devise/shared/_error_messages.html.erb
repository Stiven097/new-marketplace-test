<% if resource.errors.any? %>
<script>
  <% resource.errors.full_messages.each do |message| %>
    new Noty({
      type: 'error',
      text: '<%= message %>',
      layout: 'topRight',
      theme: 'metroui',
      timeout: '3000'
    }).show();
  <% end %>
</script>
<% end %>
